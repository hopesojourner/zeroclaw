# Ariadne — CLI Channel Configuration
#
# Exposes all three states via the local command-line interface.
# Intended for local testing, development, and operator-level access.
#
# Usage:
#   zeroclaw agent --channel cli --state operational
#   zeroclaw agent --channel cli --state companion
#   zeroclaw agent --channel cli --state administrative
#
# Administrative state requires operator token authentication even via CLI.
#
# Referenced by: agents/ariadne.yaml

adapter: "cli"

# All states are accessible through the CLI.
allowed_states:
  - operational
  - companion
  - administrative
